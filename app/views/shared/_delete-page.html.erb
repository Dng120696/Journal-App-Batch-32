<%= button_to "Delete #{obj.last.class}", url, method: :delete, data: { turbo_confirm:"Are you sure to delete this #{obj.last.class}?" }, class: 'text-sm py-2 px-4 bg-gray-200 text-gray-500 rounded-md font-semibold hover:cursor-pointer hover:bg-gray-300 hover:text-gray-800' %>
