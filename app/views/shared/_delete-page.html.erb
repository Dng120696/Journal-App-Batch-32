<%= button_to "Delete #{obj.last.class}", url, method: :delete, data: { turbo_confirm:"Are you sure to delete this #{obj.last.class}?" }, class: 'bg-gray-200 rounded-sm font-semibold py-2 px-6 border-[1px] cursor-pointer text-sm text-gray-700' %>
